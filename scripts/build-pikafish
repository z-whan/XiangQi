#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/../vendor/Pikafish/src"

ARCH="${ARCH:-apple-silicon}"
TARGET="${TARGET:-build}"

echo "Building Pikafish: make -j ${TARGET} ARCH=${ARCH}"
make -j "${TARGET}" ARCH="${ARCH}"
echo "Pikafish executable: $(pwd)/pikafish"
